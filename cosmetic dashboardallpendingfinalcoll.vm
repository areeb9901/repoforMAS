<style>
    /* Adjusts only the default DataTables sort icons — nothing else */
    #PendingFinalApprovalTable thead th.sorting,
    #PendingFinalApprovalTable thead th.sorting_asc,
    #PendingFinalApprovalTable thead th.sorting_desc {
        /* Bring the arrow closer to the text */
        background-position: center right 6px !important;
        padding-right: 16px !important;
    }

    /* Match the header text color for both text and icon */
    #PendingFinalApprovalTable thead th {
        color: #333 !important; /* same tone as the rest of the headers */
    }

    /* Optional fine-tune (some DataTables themes use slightly transparent icons) */
    #PendingFinalApprovalTable thead th.sorting,
    #PendingFinalApprovalTable thead th.sorting_asc,
    #PendingFinalApprovalTable thead th.sorting_desc {
        opacity: 0.9; /* make arrow darker like the header */
    }
</style>