<head>

<link rel="stylesheet" href="$request.contextPath/js/jquery-ui-1.11.4/css/smoothness/jquery-ui.css?ver=1.0"/>

<style>
    /* keep the existing DataTables icon, just bring it closer to the text */
    #PendingFinalApprovalTable thead th.sorting,
    #PendingFinalApprovalTable thead th.sorting_asc,
    #PendingFinalApprovalTable thead th.sorting_desc {
        /* a bit less right-padding so the icon sits nearer the header text */
        padding-right: 18px;          /* tweak this up/down if needed */

        /* move the existing icon slightly left, vertically centered */
        background-position: center right 6px;
    }

    /* make header text (and any currentColor-based icon) slightly darker,
       matching the other table headers */
    #PendingFinalApprovalTable thead th.table-th-portlets-collateral {
        color: #444;                  /* adjust if your theme uses another value */
    }
</style>

</head>

<script type="text/javascript">
var j = jQuery;
jQuery.noConflict();
</script>