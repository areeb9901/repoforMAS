Hi Team,

We have reviewed the SQL error encountered while approving the collateral document for Master Agreement 513871.

The issue is occurring because the value being inserted into the "COMMENTS" column of "LDM_TB_COLL_DOC_STATUS" is 262 bytes, whereas the column is currently defined as "VARCHAR2(250 BYTE)".

We encountered a similar issue previously for the Executed Documentation functionality under BEADEV-12833.

To unblock the user, we propose increasing the size of the "COMMENTS" column from 250 bytes to 500 bytes.

Before proceeding with the database change, I would like to seek confirmation/approval from the BA team. Once approved, I can provide the required DB script for implementation.

Please confirm if we can proceed with this change.

Best regards,
Areeb Faiz