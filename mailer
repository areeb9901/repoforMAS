I have one remaining issue in the Agreement Retriever filter screen related to the new filter:

Has Derivative MA linked to Execution MA (ISDA/CDEA)

This filter has three radio options:

- Yes
- No
- Either

The filtering logic itself is working correctly. If I select Yes or No, the expected MAs are retrieved properly.

However, there are two UI/state issues that need to be fixed.

1. Default selection should be "Either"

When the Agreement Retriever screen is opened for the first time, the existing filters under the Miscellaneous section have Either selected by default, and the corresponding radio button is visibly filled.

The new filter:

Has Derivative MA linked to Execution MA (ISDA/CDEA)

should behave in exactly the same way.

Currently, when the screen is opened initially, all three radio buttons — Yes, No, and Either — appear unselected.

Expected behaviour:

On the initial opening of the Agreement Retriever screen:

"Has Derivative MA linked to Execution MA (ISDA/CDEA) = Either"

and the Either radio button should be visibly selected/filled.

2. Selected value should persist when reopening the filter panel

Currently, if I select:

"Has Derivative MA linked to Execution MA (ISDA/CDEA) = Yes"

and perform the search, the search results are correct.

However, when I open the filter screen/panel again, all three radio buttons for this filter appear empty.

The same issue occurs when No is selected.

Expected behaviour:

The filter UI should retain and display the currently applied value.

For example:

- If the user selected Yes, reopening the filter screen should show the Yes radio button selected.
- If the user selected No, reopening the filter screen should show the No radio button selected.
- If the user selected Either, reopening the filter screen should show the Either radio button selected.

Important

Please compare the implementation of this new filter with the existing Yes / No / Either radio filters under the Miscellaneous section and follow the same mechanism for:

1. Default initialization
2. Request/filter parameter binding
3. Radio button "checked" state
4. Retaining the selected value after search/filter submission
5. Reset/default-filter behaviour

Do not change the existing search/filtering logic because Yes and No are already retrieving the correct MAs.

The required fix is specifically for the UI selection state and persistence of the radio buttons.

Please identify the relevant Velocity/VM, Java/filter model/action, JavaScript, or request parameter handling involved and implement the fix consistently with the existing Miscellaneous filters.