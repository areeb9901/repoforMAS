One thing I would recommend before sending this: quickly verify in the code that the search is indeed using Lucene's default relevance sort and not applying a secondary sort later in the flow. The explanation about "not sorted by creation/execution date" is likely correct, but it's worth confirming since this is the key business question the BA asked.